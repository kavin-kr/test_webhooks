version: v0
kind: Pipeline
name: Default
services: []
steps:
  - name: Decision
    when:
      branch:
        - ci-test
      event:
        - commit
    then: node1
    else: node1
  - name: node1
    image: alpine
    commands:
      - echo "building"
