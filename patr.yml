version: v0
kind: Pipeline
name: Default
services: []
steps:
  - name: env
    image: alpine
    commands:
      - printenv | sort
    environment:
      var1:
        from_secret: hello
      var2:
        from_secret: world
